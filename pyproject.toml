[project]
name = "sprint-tracker"
version = "0.1.0"
description = "JIRA Sprint Tracker with Flask UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph",
    "langchain-openai",
    "pydantic",
    "requests",
    "python-dotenv",
    "flask"
]
